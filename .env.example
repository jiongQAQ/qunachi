# 高德地图 Web 服务 Key（从高德开放平台申请）
# 如果不配置此值，API 将使用 Mock 数据模式
AMAP_WEB_SERVICE_KEY=

# 是否使用 Mock 数据模式（可选，默认无 key 时自动使用 mock）
# 设置为 0 可强制关闭 mock
USE_MOCKS=

# 服务器端口
PORT=3000

# 数据库（Prisma）
DATABASE_URL="file:./dev.db"
